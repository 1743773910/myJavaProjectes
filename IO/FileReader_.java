package IO;

public class FileReader_ {
    public static void main(String[] args) {
    /*
        new FileWriter(file, 加上true为追加模式);
        fileWriter.write() 写入单个字符
        .write(char[]) 写入指定数组,后面可以追加start和len的参数
        .write(String) 写入整个字符串,后面可以追加参数
        相关API：toCharArray: 将String转换成Char[]
    */
    }


}
