package intger_string;

public class some_func {
    // 最大最小
    Integer n1 = Integer.MIN_VALUE;
    Integer n12 = Integer.MAX_VALUE;

    // 判断是否为数字、字母、大写、小写、空格
    /*
    Character.isDigit('a');
    Character.isLetter('a');
    Character.isUpperCase('a'); // is改为to时能转成大小写
    Character.isLowerCase('a');
    Character.isWhitespace('a');
     */

}
